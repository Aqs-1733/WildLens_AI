# 安全与隐私

- 密钥仅由FastAPI读取，前端和Electron中不得存放ARK或LangSmith密钥。
- `.env` 已被Git忽略；`.env.example` 不含真实密钥。
- 密码使用Argon2哈希，JWT设过期时间。
- 监管注册需要邀请码；生产环境应改为管理员邀请和审计。
- 视频限制MIME、扩展名和大小，并使用随机文件名。
- 公众分享隐藏珍稀物种精确位置。
- 人员检测只标记“疑似闯入”，不进行人脸识别和身份推断。
- AI回答区分观察事实、模型推测与通用知识。
- 生产部署应启用HTTPS、轮换SECRET_KEY、限制CORS并配置备份。
